# 任务计划：Pi消息直传渲染收敛
<!-- 
  内容：这是你整个任务的路线图。将其视为你的“磁盘上的工作记忆”。
  目的：在进行 50 多次工具调用后，你可能会忘记最初的目标。此文件可保持目标清晰。
  时机：在开始任何工作之前首先创建此文件。在每个阶段完成后进行更新。
-->

## 目标
<!-- 
  内容：简短的一句话，描述你试图实现的目标。
  目的：这是你的指路明灯。阅读此内容可让你专注于最终状态。
  示例：“创建一个具有添加、列出和删除功能的 Python CLI 待办事项应用。”
-->
统一前后端 Pi 原始消息协议，让 Web 端仅在渲染层包一层 `UiConversationMessage`，完成消息直传、过程消息折叠展示，并通过 eslint 与 typecheck。

## 当前阶段
<!-- 
  内容：你当前正在进行的阶段（例如，“阶段 1”，“阶段 3”）。
  目的：快速参考你在任务中所处的位置。随着进度的推进更新此内容。
-->
阶段 5

## 阶段列表
<!-- 
  内容：将任务分解为 3-7 个逻辑阶段。每个阶段都应该是可完成的。
  目的：将工作分解为多个阶段可以防止负担过重，并使进度可见。
  时机：完成每个阶段后更新状态：pending → in_progress → complete
-->

### 阶段 1：需求与发现
<!-- 
  内容：了解需要做什么并收集初始信息。
  目的：在不了解情况的情况下开始会导致劳而无功。此阶段可防止这种情况。
-->
- [x] 理解用户意图
- [x] 确定约束和需求
- [x] 在 findings.md 中记录发现
- **状态：** complete
<!-- 
  状态值：
  - pending：尚未开始
  - in_progress：当前正在进行中
  - complete：已完成此阶段
-->

### 阶段 2：规划与结构
<!-- 
  内容：决定如何解决问题以及将使用什么结构。
  目的：良好的规划可以防止返工。记录决策，以便你记住选择它们的原因。
-->
- [x] 定义技术方案
- [x] 如果需要，创建项目结构
- [x] 记录决策及其依据
- **状态：** complete

### 阶段 3：实现
<!-- 
  内容：实际构建/创建/编写解决方案。
  目的：这是工作发生的地方。如果需要，可分解为更小的子任务。
-->
- [x] 逐步执行计划
- [x] 在执行前将代码写入文件
- [x] 进行增量测试
- **状态：** complete

### 阶段 4：测试与验证
<!-- 
  内容：验证一切工作正常并符合需求。
  目的：及早发现问题可以节省时间。在 progress.md 中记录测试结果。
-->
- [x] 验证是否满足所有需求
- [x] 在 progress.md 中记录测试结果
- [x] 修复发现的任何问题
- **状态：** complete

### 阶段 5：交付
<!-- 
  内容：最终审查并移交给用户。
  目的：确保没有遗忘任何内容，且交付物完整。
-->
- [x] 审查所有输出文件
- [x] 确保交付物完整
- [ ] 交付给用户
- **状态：** in_progress

## 关键问题
<!-- 
  内容：任务期间需要回答的重要问题。
  目的：这些问题指导你的研究和决策。在进行过程中回答它们。
  示例： 
    1. 任务是否应在会话之间持久化？（是 - 需要文件存储）
    2. 存储任务的格式是什么？（JSON 文件）
-->
1. `PiMessage` 原始协议与前端 `UiConversationMessage` 包装边界应该落在哪一层？
2. `usePiChat` 与 `usePiChatCore/usePerSessionChat` 两套消息入口如何一起收敛，避免一边升级一边残留旧类型？

## 已做决策
<!-- 
  内容：你做出的技术和设计决策及其背后的原因。
  目的：你会忘记为什么做出某些选择。此表可帮助你记住并证明决策的合理性。
  时机：每当你做出重大选择（技术、方法、结构）时更新。
  示例：
    | 使用 JSON 存储 | 简单、人类可读、Python 内置支持 |
-->
| Decision | Rationale |
|----------|-----------|
| 新建 `@pi/protocol` 统一导出会话、消息、资源与流式协议类型 | 服务端与前端都在重复定义同一批契约，必须抽成单一来源 |
| 前端只在展示/乐观态层引入 `UiConversationMessage` | `pending/localId` 是 UI 元数据，不应污染服务端与协议层 `PiMessage` |
| 过程消息改成按轮次折叠显示 | 最终回答与执行过程分层展示，消息流更简洁，且契合“消息直传” |

## 遇到的错误
<!-- 
  内容：遇到的每个错误、尝试次数以及如何解决的。
  目的：记录错误可防止重复同样的错误。这对于学习至关重要。
  时机：发生错误时立即添加，即使你很快修复了它。
  示例：
    | FileNotFoundError | 1 | 检查文件是否存在，如果不存在则创建一个空列表 |
    | JSONDecodeError | 2 | 显式处理空文件情况 |
-->
| Error | Attempt | Resolution |
|-------|---------|------------|
| `usePiChat` 与 `usePiChatCore` 新旧消息模型混用 | 1 | 把 `SessionSnapshot/ChatMessage` 残留点替换为 `UiSessionSnapshot/UiConversationMessage`，并统一使用包装函数 |
| Vue 模板里的类型收窄失败 | 1 | 把 `v-if + v-for` 和链式判断改成 computed 派生，减少模板内联合类型推断压力 |

## 注意事项
<!-- 
  提醒：
  - 随着进度推进更新阶段状态：pending → in_progress → complete
  - 在做出重大决策前仔细阅读此计划（注意力控制）
  - 记录所有错误 - 它们有助于避免重复错误
  - 绝不重复失败的操作 - 而是改进你的方法
-->
- 随着进度推进更新阶段状态：pending → in_progress → complete
- 在做出重大决策前仔细阅读此计划（注意力控制）
- 记录所有错误 - 它们有助于避免重复错误
- 绝不重复失败的操作 - 而是改进你的方法
