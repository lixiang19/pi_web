# 每日巡查报告：2026-05-13

> **口径声明**：本报告按用户明确口径——**当前完成验收范围截止到任务 13**，对任务 1–13 做符合性核查，并把任务 14+ 的代码/文档痕迹列为**范围漂移/超前实现痕迹**，不将其纳入当前必须完成项。不触碰任何代码、测试或其他文档。

---

## 1. 巡查范围与验证方式

### 1.1 核心文档核验

| 路径 | 用途 |
|------|------|
| `文档/功能开发/index.md` | 功能 00–38 状态矩阵，当前口径截止至 13 |
| `文档/项目设计/开发计划与里程碑.md` | 任务 1–13 覆盖 M0、M1、M2，以及 M3 的闪念 DB / Web 入口 / 临时附件生命周期 |
| `文档/功能开发/归档/10-任务处理会话与任务Agent.md` | 归档声明实现完成 |
| `文档/功能开发/归档/13-闪念临时附件生命周期.md` | 归档声明实现完成，前端展示附件列表 |
| `文档/记忆/MEMORY.md` | 教训与真源记录 |

### 1.2 核心代码核验

| 路径 | 核验内容 |
|------|----------|
| `packages/server/src/routes/workspace-tasks.ts` | 任务处理会话 API（`GET/POST /:taskId/processing-session`） |
| `packages/server/src/index.ts` | 会话创建处禁止分叉守卫（行 950–960） |
| `packages/web/src/components/workspace/InboxView.vue` | 闪念卡片附件列表渲染（行 402–413）及附件处理按钮（行 442–445） |
| `packages/server/src/routes/fleeting.ts` | 闪念 API、附件路由、`/capture` 桌面采集路由（行 307–355） |
| `packages/web/src/lib/api.ts` | `captureFromDesktop` 调用 `/api/fleeting/capture` |
| `packages/desktop/src-tauri/src/main.rs` | 菜单栏采集命令（`capture_browser_url`、`capture_clipboard`、`capture_screenshot_region` 等） |
| `packages/server/src/db/migrations.ts` | 表结构： `automation_rules` / `automations` / `automation_runs`、`session_index.readonly` / `sessions.archived` |
| `packages/server/src/session-payload.ts` | `archived` 字段读取来源 |
| `packages/server/src/automations.ts` | 自动化规则 CRUD 与调度器 |

### 1.3 验证方式

本次为**静态代码与文档比对巡查**，未运行 `npm run check` 或测试套件，仅通过文件读取与搜索确认实现痕迹。测试引用以各归档文档自述的测试通过记录为准。

---

## 2. 任务 1–13 符合性总表

| 任务 | 导航 | API | DB | 前端 | 测试 | 归档 | 是否符合 |
|------|------|-----|------|------|------|------|----------|
| 00 工作空间初始化 | ✅ | ✅ | ✅ | ✅ | ✅ | - | ✅ |
| 01 ridge DB 基础表与迁移框架 | - | ✅ | ✅ | - | ✅ | - | ✅（无 UI 基座能力） |
| 02 隐藏 ridge 目录与路径边界 | - | ✅ | ✅ | - | ✅ | - | ✅（无 UI 基座能力） |
| 03 工作台 Shell 与标签系统 | ✅ | - | - | ✅ | ✅ | - | ✅（纯前端能力） |
| 04 左侧导航固定入口与会话列表 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 05 工作空间主页与会话创建 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 06 会话索引归档与只读状态 | ✅ | ✅ | ✅ | ✅ | ✅ | - | ✅ |
| 07 会话界面与右侧工作侧栏 | ✅ | - | - | ✅ | ✅ | ✅ | ✅ |
| 08 任务里程碑数据模型 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 09 任务列表详情与状态流转 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **10 任务处理会话与任务 Agent** | **-** | **-** | **-** | **-** | **-** | **-** | **❌ 代码已完成，index.md 漏标** |
| 11 规划工具与完成确认边界 | - | ✅ | - | - | ✅ | ✅ | ✅（无 UI 后端能力） |
| 12 闪念数据模型与 Web 入口 | ✅ | ✅ | ✅ | ✅ | ✅ | - | ✅ |
| **13 闪念临时附件生命周期** | **✅** | **✅** | **✅** | **✅** | **✅** | **✅** | **✅** |

### 2.1 说明

- **任务 10**：`workspace-tasks.ts` 已实现 `GET/POST /:taskId/processing-session`，含离线项目 409 拦截、已有会话直接返回、原子设置 `processing_session_id`；`index.ts` 行 950–960 含禁止分叉守卫（检查 `workspace_tasks.processing_session_id` 并返回 409）。归档文档明确记录实现完成。**`index.md` 全部维度标 `-` 与代码真源不符，属不符合项。**
- **任务 13**：`fleeting_attachments` 表在 migration v8；后端 `fleeting-attachments.ts` 含 store/get/delete/migrate；`fleeting.ts` 含上传/列表/清理/迁移全链路；前端 `InboxView.vue` 行 402–413 确实渲染 `getNoteAttachments(note.id)` 的附件列表（图标+文件名+大小），行 442–445 有“附件”处理按钮。**前端展示已闭环，index.md 标记准确。**

---

## 3. 明确不符合项与 Bug / 风险（按 P0 / P1 / P2）

### P0 — 状态矩阵与代码真源严重脱节

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| **P0-1** | **任务 10 在 `index.md` 全维度标 `-`，但代码已实现** | `index.md` 第 17 行；`workspace-tasks.ts` 行 153–249 含完整处理会话 API；`index.ts` 行 950–960 含禁止分叉守卫；归档文档 `10-任务处理会话与任务Agent.md` 声称实现完成 | 团队可能误判任务 Agent/处理会话未启动，影响排期与资源分配；状态矩阵失去可信力 |

### P1 — 数据模型与设计债务

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| **P1-1** | **`session_index.readonly` 与业务真源 `sessions.archived` 未统一** | `migrations.ts` 同时定义 `sessions.archived`（行 72）与 `session_index.readonly`（行 95、402-403）；`session-payload.ts` 行 278 读取 `lookup.archived`，归档后消息 403 依赖 `sessions.archived` | 数据模型存在双字段冗余，未来修改易遗漏一侧，导致只读/归档状态不一致 |
| **P1-2** | **`automation_rules` 与 `automations` / `automation_runs` 双轨模型未收敛** | `migrations.ts` 行 130–176 同时存在 `automation_rules`（被 `automations.ts` 业务消费）与 `automations` + `automation_runs`（无业务消费）；`automations.ts` 仅操作 `automation_rules` | 后续任务（M9 自动化运行历史、失败通知）面临模型债务，需先做表合并或废弃决策 |
| **P1-3** | **工作台占位入口暴露未实现功能** | `WorkspacePage.vue` 中 search / notifications / skills / space_preview 走 `WorkspaceFeaturePlaceholder` | 用户点击后看到空白占位，体验差；对应功能编号 24 / 34 / 32 / 21 尚未实现但入口已存在 |

### P2 — 可观测性与可维护性风险

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| **P2-1** | **E2E 测试硬编码 URL / 密码 / 项目名** | `session-interface-sidebar.spec.ts` 硬编码 `127.0.0.1:5175` / `ridge-admin`；`workspace-left-nav-task04.spec.ts` 硬编码 `AuroraPlatformWeb` | CI 或他机不可直接运行，质量门禁形同虚设 |
| **P2-2** | **`fleeting.ts` 分析 runner 静默吞错** | 行 342：`void Promise.resolve(getAnalysisRunner?.()?.run(id)).catch(() => undefined)` | 闪念分析失败无日志、无通知、无重试提示，排障困难 |
| **P2-3** | **MEMORY.md 功能编号 13 语义冲突易误导** | `MEMORY.md` 行 278 记录“旧 2026-05-01 'task #13 任务 SQLite 真源最小实现' 说法已废弃，功能编号 13 现在是闪念临时附件生命周期” | 历史编号与当前编号混用，后续开发者/AI 可能按错误记忆做假设；文档本身做了澄清，但仍属认知风险 |
| **P2-4** | **多个文件接近 1500 行阈值** | `index.ts` 1415 行、`api.ts` 1249 行、`session-context.ts` 1122 行、`WorkspacePage.vue` 1046 行 | 尚未超标，但随着 M3+ 功能接入，持续增长后需拆分 |
| **P2-5** | **`DashboardView.vue` 疑似未使用** | `WorkspacePage.vue` 中 search 等入口走 `WorkspaceFeaturePlaceholder`，未找到 `DashboardView` 被渲染的证据 | 维护成本与理解干扰（需进一步确认是否死代码） |

---

## 4. 后续任务范围漂移记录（任务 14+ 的代码/文档痕迹）

> 以下代码/文档在 working tree 中**已存在**，但按“当前完成到任务 13”的口径，属于**范围漂移/超前实现痕迹**，不应纳入当前验收范围，亦不能作为“必须已完成项”来评判。

| 任务 | 漂移痕迹 | 证据 |
|------|----------|------|
| **14 桌面采集入口** | 服务端 `/api/fleeting/capture`（`fleeting.ts` 行 307–355）；前端 `api.ts` 有 `captureFromDesktop`；桌面端 `main.rs` 有完整菜单栏采集命令（文字/剪贴板/区域截图/窗口截图/全屏截图/文件/选区/浏览器网址/录音） | 按 M3 里程碑应属任务 14；当前口径下不应验收，但也不能说“未实现” |
| **15 fleeting agent 分析建议** | `fleeting.ts` 中 `getAnalysisRunner` 调用、后台 worker 轮询、`analysis_status` 字段；归档文档 `15` 未找到，但代码痕迹明显 | 同属 M3 后续，不应纳入任务 13 验收 |
| **16 闪念处理为正式对象** | `fleeting.ts` 已实现 `process/journal`、`process/clip`、`process/task`、`process/milestone`、`process/attachment` 五种路径；`index.md` 标为 ✅ 且有归档 | 用户口径中 16 不在当前范围，但代码已超前实现 |
| **17 文件页与正式附件目录** | `FilesView.vue` 已接入 `WorkspacePage.vue`，非占位；`index.md` 标 ⚠️ 但代码证据显示已有真实文件树浏览 | 属 M4 超前痕迹 |
| **18 文件处理状态与临时文件边界** | `PATCH /status`、`POST /retry` API 完整；`index.md` 标 DB✅ | 同属 M4 超前痕迹 |
| **36 自动化规则** | `automations.ts` 完整 CRUD + 调度器；`index.md` 标 ◐ | 属 M9 超前痕迹，且与双轨模型债务并存 |

---

## 5. 长文件检查

扫描范围：`packages/server/src`、`packages/web/src`、`packages/protocol/src`、`packages/desktop/src-tauri/src` 内的 `.ts` / `.vue` / `.rs` 文件。

**结果：未找到超过 1500 行的文件。**

接近阈值文件：

| 路径 | 行数 | 状态 |
|------|------|------|
| `packages/server/src/index.ts` | 1415 | 接近阈值，需关注持续增长 |
| `packages/web/src/lib/api.ts` | 1249 | 接近阈值 |
| `packages/server/src/session-context.ts` | 1122 | 接近阈值 |
| `packages/web/src/pages/WorkspacePage.vue` | 1046 | 接近阈值 |

---

## 6. 优化建议

1. **立即修正 `index.md` 状态矩阵**：
   - 任务 10 至少更新为 `◐`（已有代码实现和测试覆盖，前端维度待确认是否完整接入 `TaskView` 处理按钮），绝不应继续标 `-`。

2. **收敛 `session_index.readonly` vs `sessions.archived` 真源**：
   - 评估是否可废弃 `session_index.readonly`，统一使用 `sessions.archived` 作为归档/只读真源；或反之明确分工并补全同步逻辑。

3. **决策自动化双轨模型**：
   - 明确 `automation_rules` 为唯一正式表，或迁移到 `automations` + `automation_runs`；删除废弃表/字段，避免 M9 启动时背负模型债务。

4. **修复 E2E 可移植性**：
   - 用 `process.env.BASE_URL` / Playwright project config 替代硬编码 URL；
   - 用 API 预造数据替代依赖已有会话/项目名。

5. **处理占位入口策略**：
   - 为 search(24)、notifications(34)、skills(32)、space_preview(21) 制定显式计划：要么实现最小可用版本，要么在界面临时隐藏入口，避免用户点击空白占位。

6. **提升可观测性**：
   - `fleeting.ts` 行 342 的 `.catch(() => undefined)` 改为至少记录 `console.error` 或写入后台日志，避免分析失败完全静默。

7. **关注文件体积**：
   - 在 `index.ts` 超过 1500 行或 `api.ts` 超过 1200 行前，规划按功能域拆分（如将路由装配拆到各子模块的 `mountRoutes` 函数）。

---

## 7. 后续建议清单（按优先级）

| 优先级 | 事项 | 关联任务/里程碑 |
|--------|------|----------------|
| 🔴 高 | 修正 `index.md` 任务 10 状态 | 10 / M2 |
| 🔴 高 | 统一 `archived` / `readonly` 真源 | 06 / M1 |
| 🟡 中 | 决策并收敛自动化双轨模型 | 36 / M9 |
| 🟡 中 | 修复 E2E 硬编码与可移植性 | 全局测试 |
| 🟡 中 | 为工作台占位入口制定显式计划 | 21/24/32/34 |
| 🟢 低 | 替换 fleeting 分析吞错为可观测日志 | 13 / M3 |
| 🟢 低 | 确认 `DashboardView.vue` 是否死代码 | 全局清理 |
| 🟢 低 | 监控长文件体积，规划拆分 | 全局维护 |

---

## 8. 搜索与核验依据

本次巡查使用的关键搜索与读取策略：

1. **文档总览**：读取 `文档/功能开发/index.md`、`文档/项目设计/开发计划与里程碑.md`、各归档文档。
2. **任务 10 核验**：读取 `packages/server/src/routes/workspace-tasks.ts` 行 153–249（`processing-session` API）；读取 `packages/server/src/index.ts` 行 940–969（禁止分叉守卫）。
3. **任务 13 核验**：读取 `packages/web/src/components/workspace/InboxView.vue` 行 390–450（附件列表渲染 `getNoteAttachments` 及附件处理按钮）。
4. **任务 14 痕迹**：读取 `packages/server/src/routes/fleeting.ts` 行 307–355（`/capture` 路由）；确认 `packages/web/src/lib/api.ts` 有 `captureFromDesktop`；读取 `packages/desktop/src-tauri/src/main.rs` 菜单栏采集命令与事件分发。
5. **数据模型核验**：grep `migrations.ts` 中 `automation_rules` / `automations` / `automation_runs`、`readonly` / `archived`；读取 `session-payload.ts` 确认 `archived` 读取来源。
6. **风险点 grep**：`.catch(() => undefined)` 在 `fleeting.ts`；`WorkspaceFeaturePlaceholder` 在 `WorkspacePage.vue`；硬编码 URL/密码在 E2E 测试文件。
7. **长文件扫描**：`find packages/server/src packages/web/src packages/protocol/src packages/desktop/src-tauri/src -type f \( -name "*.ts" -o -name "*.vue" -o -name "*.rs" \) -exec wc -l {} + | sort -rn | head -20`

---

> **备注**：本报告为纯观察记录，**未修改任何源代码、测试、配置或其他文档**。

*报告生成时间：2026-05-13*  
*巡查人：AI Agent（基于 current working tree 静态分析与文档比对）*
