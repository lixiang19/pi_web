# 每日巡查报告：2026-05-12

> **声明**：本报告基于当前 working tree（未区分已暂存/未暂存/未跟踪），不代表 HEAD 状态。仓库已有大量业务代码与文档的未提交/未跟踪改动，本次巡查**不触碰任何代码**，仅做状态记录与问题标记。

---

## 1. 巡查范围与依据

| 类别 | 关键文件 |
|------|----------|
| 功能总览 | `文档/功能开发/index.md` |
| 设计记忆 | `文档/记忆/MEMORY.md` |
| 闪念系统 | `文档/功能开发/13-闪念临时附件生命周期.md`、`16-闪念处理为正式对象.md` |
| 闪念 API | `packages/server/src/routes/fleeting.ts` |
| 工作台主入口 | `packages/web/src/pages/WorkspacePage.vue` |
| 设置页 | `packages/web/src/components/workspace/SettingsTabContent.vue` |
| 自动化后端 | `packages/server/src/automations.ts` |
| 数据库 schema | `packages/server/src/db/migrations.ts`、`packages/server/src/db/index.ts` |
| 会话索引/归档 | `packages/server/src/db/migrations.ts`（`session_index.readonly`、`sessions.archived`） |
| 仪表盘 | `packages/web/src/components/workspace/DashboardView.vue` |
| E2E 测试 | `packages/web/e2e/session-interface-sidebar.spec.ts`、`workspace-left-nav-task04.spec.ts`、`parallel-dev-roadmap.spec.ts` |
| Playwright 配置 | `packages/web/playwright.config.ts` |
| Git 状态 | `git status --short`（大量 M/??/D） |

---

## 2. 总体判断

当前 working tree 处于**“入口与骨架大量就位，核心闭环大量缺失”**的阶段。左侧导航、分屏框架、会话列表、任务看板、闪念列表、自动化规则 CRUD、设置外观等已具备可用形态，但以下关键闭环尚未完成：

- 闪念→任务/里程碑/附件的正式处理链路（16）
- 闪念临时附件目录与生命周期（13）
- 自动化运行记录与通知中心（36/34）
- 搜索/通知/文件/Skill 等功能页面（24/34/17/32）
- 设置页的备份恢复与数据路径（37/M10）
- E2E 测试的 CI 可移植性

`index.md` 中的 ✅ 容易在“局部端到端”场景下被过度解读为功能完成，需要更细粒度的状态拆分。

---

## 3. 已完成/已修复项复核（对比 2026-05-11 巡查）

| 项 | 状态 | 证据 |
|----|------|------|
| `/api/fleeting` 路由挂载 | ✅ 已确认 | `packages/server/src/routes/fleeting.ts` 存在且被使用 |
| 自动化 getter/初始化调整 | ✅ 已确认 | `automations.ts` 中 `listDueRules`/`updateNextRunAt` 等已就位 |
| `/api/files/create` 先校验再 IO | ✅ 已确认 | 相关安全顺序已调整 |
| 首页附件上传失败调用 `deleteSession` 清理 | ✅ 已确认 | `WorkspacePage.vue` `handleHomeSubmit` 中上传失败分支已调用 `deleteSession` |
| 生产密码环境变量接入 | ✅ 已确认 | 安全相关配置已接入 |
| 任务项目绑定/筛选接入 | ✅ 已确认 | `WorkspacePage.vue` 中项目列表与会话过滤逻辑已存在 |
| `clips` 进入 migration v7 | ✅ 已确认 | `packages/server/src/db/migrations.ts` 中 `RIDGE_DB_SCHEMA_VERSION = 7` 且含 `clips` 表 |

---

## 4. 与开发计划/功能状态不一致

### 4.1 `index.md` 状态矩阵过度表达

`文档/功能开发/index.md` 对多个功能标记了 ✅，但工作树显示它们仅完成了**部分维度**：

| 功能 | index.md 标记 | 实际状态 |
|------|---------------|----------|
| **13 闪念临时附件生命周期** | 导航✅ API✅ DB✅ 前端✅ | 仅闪念列表/创建/删除 API 可用；**临时附件目录、引用关系、迁移清理均未闭环** |
| **16 闪念处理为正式对象** | 导航✅ API✅ DB✅ 前端✅ | 仅 journal/clip 处理可用；**task/milestone/attachment 处理未实现**，`process/task` 仅返回 202 |
| **36 自动化规则运行与跳过** | 导航✅ API✅ DB✅ 前端✅ 测试✅ | 仅 `automation_rules` 的 CRUD 与调度器存在；**运行记录、跳过记录、失败通知未闭环** |
| **37 备份恢复设置主题收尾** | 导航✅ 前端✅ | 仅设置页入口与外观/偏好存在；**备份/恢复/数据路径/错误边界均未实现** |
| **07 会话界面与右侧工作侧栏** | 导航✅ 前端✅ | 右侧侧栏框架存在，但 **API/DB/测试/E2E 均标为 `-`**；`index.md` 未标 ✅，但“归档”文档被移动，存在状态混乱 |
| **24 全局搜索资产导航器** | 导航✅ | 仅搜索入口按钮存在，点击后走 `WorkspaceFeaturePlaceholder` |
| **34 通知与建议中心** | 导航✅ | 仅通知入口按钮存在，无 API/UI |

**建议**：`index.md` 应增加“部分完成/占位/待测试/E2E/归档”等子状态，或把上述功能的非 ✅ 维度明确标为 `⚠️`。

### 4.2 `MEMORY.md` 任务系统记忆过期

`文档/记忆/MEMORY.md` 第 190–195 行仍记载：

> “task #13 是任务系统当前唯一 canonical 实现任务……写入 `<workspace>/.ridge/ridge.db` 的 `tasks` 表”

这与当前 working tree 的设计**严重冲突**：
- 任务系统实际数据位于 **`~/.pi/ridge.db`**（全局），而非 workspace 本地。
- 表名为 **`workspace_tasks`** / **`workspace_milestones`**，而非 `tasks`。
- 功能开发编号 **#13 的实际文档是“闪念临时附件生命周期”**，与“任务系统 SQLite 真源”语义冲突。

**风险**：后续开发者或 AI 可能依据 MEMORY.md 做出错误的数据库/路径假设。

### 4.3 闪念处理为正式对象（16）未闭环

`文档/功能开发/16-闪念处理为正式对象.md` 明确要求：
- 处理为任务、里程碑、剪藏、正式附件
- 成功后删除原闪念
- 附件迁移到正式目录

当前 `packages/server/src/routes/fleeting.ts` 中：
- `POST /:noteId/process/journal` — 已实现（写入日记文件并删除闪念）
- `POST /:noteId/process/clip` — 已实现（写入 `clips` 表并删除闪念）
- `POST /:noteId/process/task` — **仅返回 202**：`{ processed: false, message: "任务系统正在接入中，暂不能从闪念创建任务" }`
- **milestone / attachment 处理路径不存在**
- **附件迁移逻辑不存在**

`index.md` 对 16 标记 API✅ DB✅ 容易误导为功能完成。

### 4.4 闪念临时附件生命周期（13）未落地

`文档/功能开发/13-闪念临时附件生命周期.md` 要求：
- `.ridge/fleeting-attachments` 临时目录
- 截图和文件副本上传
- 未处理闪念引用临时附件
- 删除未处理闪念时清理未引用临时附件
- 处理成功后迁移到正式附件目录

当前代码中：
- `workspace-chat.ts` 在 `RIDGE_SYSTEM_SUBDIRS` 中列出并在 `ensureWorkspaceTemplate()` 中创建 `.ridge/fleeting-attachments`**目录模板已创建/可创建**。
- 会话附件走 `/api/sessions/:sessionId/attachments`，存储于 `session_attachments` 表，**与闪念临时附件是完全独立的体系**。
- `fleeting_notes` 表及相关 API **未见任何附件引用字段**（如 `attachment_ids`、临时文件路径等）。
- **闪念附件上传 API、DB 引用关系、删除清理、成功迁移到正式附件目录均未闭环**。

**结论**：13 的目录结构已就位，但核心闭环（附件上传、引用关系、清理、迁移）尚未完成，`index.md` 中的 API✅ DB✅ 属于误标或过度乐观。

### 4.5 导航入口完成 ≠ 功能完成

`WorkspacePage.vue` 固定入口包含：闪念/搜索/通知/任务/文件/终端/自动化/Skill/设置。

实际渲染时：
- **真实组件**：`tasks` → `TaskView`、`moments` → `InboxView`、`automation` → `AutomationTabContent`、`settings` → `SettingsTabContent`
- **占位**：`search`、`notifications`、`files`、`skills` → 全部走 `WorkspaceFeaturePlaceholder`
- `space_preview` 也走 `WorkspaceFeaturePlaceholder`

左侧导航入口的 UI 存在只能说明“用户能点进去”，不能说明功能已实现。

### 4.6 SettingsTabContent.vue 与 M10 计划差距大

`SettingsTabContent.vue` 当前仅包含：
- 外观（主题/模式）
- 安全退出
- 偏好（通知开关/侧边栏折叠/语言）
- 关于（版本信息）

M10（37 备份恢复设置主题收尾）要求的**备份/恢复、数据路径配置、错误边界**等均未实现。

### 4.7 自动化运行记录与通知中心缺失

`packages/server/src/automations.ts` 中：
- 业务只读写 **`automation_rules`** 表。
- `db/migrations.ts` 与 `db/index.ts` 同时存在 **`automations`** 与 **`automation_runs`** 表定义。
- 但 `automation_runs` **无任何业务写入/查询代码**。
- `notification_events` 表仅由 `background-jobs.ts` 在 job 失败时写入，**无 `/api/notifications` 路由、无通知中心 UI**。

这意味着：
- 自动化规则的“运行历史、跳过原因、失败详情”无法查看（M9/36 未闭环）。
- 用户无法在工作台看到任何通知（34 未闭环）。

### 4.8 会话只读真源不一致

`db/migrations.ts` 中 `session_index` 表有 **`readonly INTEGER NOT NULL DEFAULT 0`** 字段，但：
- 归档只读的实际控制逻辑依赖 **`sessions.archived`** 字段。
- Message endpoint 对 archived session 返回 403。
- 这可以工作，但与 `06-会话索引归档与只读状态.md`（已归档）中“`session_index` CRUD/readonly”设计不一致。

**需要决定真源**：是继续用 `sessions.archived`，还是把只读状态迁移到 `session_index.readonly`？决定后需更新 06 文档或代码，并补测试。

### 4.9 DashboardView.vue 疑似死代码/未接入

`DashboardView.vue` 定义了 `emit("create-goal", goal)`，但：
- `grep` 显示 `create-goal` 仅在组件自身及其测试文件中出现。
- `WorkspacePage.vue` 未渲染 `DashboardView` 组件。
- 该组件属于**疑似遗留代码**，或某个未接入功能（如“目标/OKR 面板”）的半成品。

### 4.10 HomePage.vue AI 建议是占位

`HomePage.vue` 中的 AI 建议区域明确是占位文案（根据代码审查），不能将其视为“AI 建议中心”功能已完成。

---

## 5. 需要修复的 Bug / 风险（按 P0/P1/P2 分级）

### P0 — 阻塞或数据一致性风险

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| P0-1 | `index.md` 对 13/16/36 的 ✅ 过度表达 | `index.md` 第 18、21、41 行 | 团队/外部对功能完成度产生误判，导致计划偏差 |
| P0-2 | `MEMORY.md` 任务系统位置与表名过期 | `MEMORY.md` 第 190–195 行 | AI/开发者可能按错误记忆实现后续功能，导致数据写错位置 |
| P0-3 | `session_index.readonly` 与 `sessions.archived` 真源未统一 | `migrations.ts` 第 95 行 vs `session-payload.ts` 中的 403 逻辑 | 未来修改只读逻辑时可能只改一处，导致行为分裂 |

### P1 — 功能闭环缺失

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| P1-1 | 闪念 `process/task` 未实现 | `fleeting.ts` 第 301–311 行返回 202 | 用户无法将闪念转为任务，16 未闭环 |
| P1-2 | 闪念 `process/milestone` 与 `process/attachment` 不存在 | `fleeting.ts` 无对应路由 | 里程碑/附件处理完全缺失 |
| P1-3 | 闪念临时附件体系未建立 | 目录模板已创建；缺少闪念附件上传 API、DB 引用、清理与迁移逻辑 | 13 核心闭环未落地 |
| P1-4 | 自动化运行记录未写入 | `automations.ts` 无 `automation_runs` 写入 | 用户看不到自动化执行历史 |
| P1-5 | 通知中心无 API/UI | `notification_events` 仅有 background job 写入，无读取端 | 34 完全未落地 |
| P1-6 | 设置页缺少备份/恢复/数据路径 | `SettingsTabContent.vue` 无相关 section | 37 未闭环 |
| P1-7 | `DashboardView` 未接入且 `create-goal` 无消费 | 仅组件/测试引用，WorkspacePage 未渲染 | 死代码或半成品功能 |

### P2 — 质量/可维护性

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| P2-1 | 搜索/通知/文件/Skill 页面占位未清理 | `WorkspacePage.vue` 中走 `WorkspaceFeaturePlaceholder` | 用户点击后看到空白占位，体验差 |
| P2-2 | `HomePage` AI 建议占位未收敛 | 明确为占位文案 | 需决定是删除还是实现 |
| P2-3 | 功能编号语义冲突（13 既是任务系统又是闪念附件） | `MEMORY.md` 与 `文档/功能开发/13-...` | 长期维护混乱 |
| P2-4 | `parallel-dev-roadmap.spec.ts` 硬编码外部 URL | `localhost:8765` | 该测试依赖外部文档服务器，CI 不可运行 |
| P2-5 | `fleeting.ts` 中 `analysisRunner` 失败被静默吞掉 | `.catch(() => undefined)` | 分析失败无日志/无通知，排障困难 |

---

## 6. 测试与质量门禁风险

### 6.1 E2E 可移植性严重不足

以下测试在 CI 或他人机器上**大概率失败**：

| 文件 | 问题 | 具体证据 |
|------|------|----------|
| `session-interface-sidebar.spec.ts` | 硬编码 URL、密码、依赖本地已有数据 | `BASE_URL = "http://127.0.0.1:5175"`、`PASSWORD = "ridge-admin"`、依赖标题为“你好”的已存在会话 |
| `workspace-left-nav-task04.spec.ts` | 硬编码项目名、终端提示符、多处固定等待 | `AuroraPlatformWeb`、`lixiang@lixiangdeMac-mini-3 ridge-workspace %`、多处 `waitForTimeout` |
| `parallel-dev-roadmap.spec.ts` | 硬编码外部文档服务器 | `http://localhost:8765/并行开发路线图.html`、多处 `waitForTimeout` |
| `playwright.config.ts` | 固定 baseURL | `[::1]:5175`（IPv6 loopback，与 E2E 中 127.0.0.1 不一致） |

**风险**：E2E 单独运行时会受这些硬编码影响；默认根 `npm run test` 不等于 E2E（根目录为 `pnpm -r run test`，通常跑各 workspace 的 test 脚本；Playwright E2E 是 `packages/web` 的 `test:e2e`）。在干净环境或 CI 中 E2E 大概率无法通过，质量门禁形同虚设。

### 6.2 静态质量门禁已通过但仍有警告/测试未覆盖

> 注：本报告初始撰写时巡查未运行 `npm run check`，但后续 auto-verifier 已补执行并记录于第 8 节。

`npm run check`（根目录 ESLint + TypeScript）后续已运行并通过：
- **ESLint**：0 errors / 16 warnings（均为 `no-explicit-any`；涉及文件：`packages/server/src/__tests__/automation-api.test.ts`、`packages/web/src/composables/__tests__/useFileTreeActions.test.ts`、`packages/web/src/composables/useFileTreeActions.ts`）。
- **TypeScript / `vue-tsc`**：通过。

但以下测试与验证**仍未执行**，相关风险未被排除：
- `packages/server` 单测（`pnpm test`）。
- `packages/web` 单测（`pnpm test`）。
- E2E 测试（依赖外部 URL 与本地数据，CI 风险高）。
- 闪念 `process/task` 端到端验证（当前返回 202，预期失败）。
- 闪念临时附件目录的完整上传/引用/清理/迁移闭环验证。
- `automation_runs` 表写入验证。
- 通知中心 API/UI 验证。

**建议**：在下次提交前补齐 server/web 单测与 E2E 运行结果，并清零 ESLint warnings。

---

## 7. 优先级开发计划

### 短期（1–2 天）— 文档与状态收敛

1. **P0-1 / P0-2**：修正 `index.md` 状态矩阵 — 对 13/16/36/37 增加“部分完成/占位/待测试”子状态；更新 `MEMORY.md` 任务系统部分，明确 `~/.pi/ridge.db` + `workspace_tasks` 为真源，删除 `<workspace>/.ridge/ridge.db` 的旧描述。
2. **P1-7**：决定 `DashboardView` 去留 — 如果暂不接入，从代码库中移除或移到 `归档/`；如果计划后续接入，在 `index.md` 中标记为占位。
3. **P0-3**：决定 `session_index.readonly` vs `sessions.archived` 真源 — 选定单一来源后更新代码或文档，并补单元测试。

### 中期（3–7 天）— 核心闭环补齐

4. **P1-1 / P1-2**：实现闪念 `process/task` 与 `process/milestone` — 先写测试（创建任务/里程碑并删除闪念、失败时保留），再实现路由；若暂不实现，将 16 在 `index.md` 中标为部分完成。
5. **P1-3**：闪念临时附件生命周期 — 设计临时目录结构、DB 引用表、清理/迁移逻辑；先写 spec 与测试，再实现 API。
6. **P1-4 / P1-5**：自动化运行记录 + 通知中心 — 在 `automations.ts` 调度器 `tick()` 中写入 `automation_runs`；提供 `/api/notifications` 路由；前端通知入口从 `WorkspaceFeaturePlaceholder` 替换为真实 `NotificationTabContent`。
7. **P1-6**：设置页备份/恢复/数据路径 — 在 `SettingsTabContent.vue` 中新增 section，对接后端 API（若 API 不存在则先设计）。

### 长期（1–2 周）— 质量与体验

8. **P2-1 / P2-2**：清理占位入口与死代码 — 为 search/notifications/files/skills 决定实现计划或降级为“暂不实现”；清理 `HomePage` AI 建议占位文案。
9. **P2-4 / 6.1**：E2E 稳定化 — 统一使用 `process.env` / Playwright 项目配置管理 baseURL 与密码；用 API 预造会话/项目数据替代硬编码 `AuroraPlatformWeb`；用 `waitForSelector`/`waitForResponse` 替代 `waitForTimeout`；统一 baseURL（IPv6 vs IPv4）。
10. **6.2**：建立 `npm run check` 零错误门禁 — 在每次功能提交前强制通过。

---

## 8. 验证记录

> **声明**：以下验证由 auto-verifier 执行，仅更新本报告，未修改任何代码或配置。

| 验证项 | 状态 | 备注 |
|--------|------|------|
| `npm run check`（根目录 ESLint + TypeScript） | ✅ 通过 | ESLint：0 errors / 16 warnings（均为 `no-explicit-any`）；涉及文件：`packages/server/src/__tests__/automation-api.test.ts`、`packages/web/src/composables/__tests__/useFileTreeActions.test.ts`、`packages/web/src/composables/useFileTreeActions.ts`。TypeScript / `vue-tsc`：通过 |
| `cd packages/server && pnpm test` | ⏳ 未运行 | 本次未执行，需后续补充 |
| `cd packages/web && pnpm test` | ⏳ 未运行 | 本次未执行，需后续补充 |
| E2E 在干净环境运行 | ⏳ 未运行 | 依赖外部 URL 与本地数据，CI 风险高，未执行 |
| 闪念 `process/task` 端到端 | ⏳ 待验证 | 当前返回 202，预期失败，未执行测试 |
| 闪念临时附件目录存在性 | ⏳ 待验证 | 目录模板已创建，附件上传/引用/清理/迁移未闭环，未执行端到端验证 |
| `automation_runs` 表写入 | ⏳ 待验证 | 当前无业务写入，未执行测试 |
| 通知中心 API/UI | ⏳ 待验证 | 当前未实现，未执行测试 |

---

## 9. 附录：本次未提交改动的概览

根据 `git status --short`，当前 working tree 存在以下显著变动（本次不区分归属、不提交）：

- **已修改（M）**：`packages/protocol/src/index.ts`、`packages/server/src/index.ts`、`packages/server/src/session-indexer.ts`、`packages/server/src/session-payload.ts`、`packages/web/src/pages/WorkspacePage.vue` 等核心文件。
- **新增（??）**：多个 E2E 测试（`session-archive.spec.ts`、`session-interface-sidebar.spec.ts`）、自动化测试报告目录（`自动化测试/...`）、新功能文档（`38-AI对话主线闭环.md`）。
- **删除（D）**：旧归档文档被移动/删除（`07-...` 等）。

这些变动说明开发节奏活跃，但尚未收敛到可提交的干净状态。建议先完成短期文档/状态收敛，再分批提交代码改动。

---

*报告生成时间：2026-05-12*  
*巡查人：AI Agent（基于 current working tree 静态分析）*
