# 数据库与迁移

## 职责

`~/.pi/ridge.db` 是 ridge 产品层结构化真源，保存设备、项目、任务、会话索引、闪念、自动化、后台任务、搜索状态、通知和设置。

## 入口

- `packages/server/src/db/index.ts`
- `packages/server/src/db/migrations.ts`
- `initializeRidgeDb(workspaceDir?)`

## 当前迁移机制

- `ridge_meta` 保存当前 `schema_version`，当前为 `21`。
- `RIDGE_DB_MIGRATIONS` 是版本化迁移清单，启动时按 `version` 升序补齐未执行项。
- 启动时在事务中执行迁移 bookkeeping、旧库预修复、版本化迁移、二次缺列修复和 schema version 写入。
- 缺列修复使用 `PRAGMA table_info` 检查，不能只依赖 `CREATE TABLE IF NOT EXISTS`。
- 重命名列时，必须先通过 `ensureRenamedProjectSourceColumn` 在 `runPreBootstrapMigrations` 中处理旧库，再让 migration 做 no-op  bookkeeping，否则旧库会报 `no such column`。

## 核心表

- `devices`
- `projects`
- `sessions`
- `session_index`
- `workspace_tasks`
- `workspace_milestones`
- `fleeting_notes`
- `clips`
- `automation_rules`
- `background_jobs`
- `search_index_status`
- `search_chunks`
- `notification_events`
- `app_settings`
- `session_attachments`
- `fleeting_attachments`
- `file_processing_status`
- `device_bundle_acks`
- `device_bundle_served`

## 边界

- Pi 原始会话正文不进入 `ridge.db`。
- **归档真源**：会话归档状态统一以 `sessions.archived` 和 `session_index.archived` 为真源；`session_index.readonly` 已废弃并移除。
- **自动化表真源**：业务实际消费 `automation_rules` 作为正式表；`automations` / `automation_runs` 从未被业务消费，已从 bootstrap 和迁移中移除。
- 旧库存在缺列时启动自动补齐。
- 数据库迁移失败应由事务回滚，不能留下半成品 schema。
- 新增结构化表或索引时必须新增 `RIDGE_DB_MIGRATIONS` 版本，不得只改总量 bootstrap SQL。

## 当前 migration 版本变更记录

| 版本 | 说明 |
|------|------|
| 2 | session_index（移除 `readonly` 列，统一使用 `archived`） |
| 3 | workspace tasks and milestones |
| 4 | background_jobs（移除了未消费的 `automations` / `automation_runs`） |
| 5 | fleeting search and notifications |
| 6 | session_attachments |
| 7 | clips 表；workspace_tasks / workspace_milestones 增加 `project_id` 及对应索引 |
| 8 | fleeting_attachments（闪念附件） |
| 9 | desktop capture 字段补全（no-op，字段已在 bootstrap schema） |
| 10 | file_processing_status（文件处理状态：`pending | converting | converted | indexed | convert_failed | index_failed`） |
| 11 | projects.source → external_origin，语义清晰化；workspace 虚拟项目标记为 `project_type = 'workspace'` |
| 12 | python_conversion_jobs（Python 通用转换服务异步任务跟踪） |
| 13 | search_chunks（RAG chunk 内容索引） |
| 14 | 标准 RAG chunk metadata 与 refresh_policy 索引 |
| 15 | 本地 RAG embedding vector 索引 |
| 16 | 设备 `token_hash` 索引；projects 同设备同路径唯一索引 |
| 17 | device_bundle_acks（runtime bundle ack 基础表） |
| 18 | bundle ack hash/version/sync_status 与 device_bundle_served |
| 19 | device_bundle_served materialized 状态 |
| 20 | device_bundle_served materialized_hash 校验字段 |
| 21 | 合并收敛迁移：幂等补齐 RAG、设备 token hash 与 runtime bundle 表/索引 |
