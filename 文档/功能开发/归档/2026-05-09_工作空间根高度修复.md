# 工作空间根高度修复

## 背景

工作空间根容器使用 `h-full`，但 `html/body/#app` 只有 `min-height: 100%`，高度链不稳定，导致 `#app` 下的工作台无法可靠撑满屏幕。

## 修复

- `WorkspacePage.vue` 根容器从 `h-full` 改为 `h-screen`，直接绑定视口高度。
- `style.css` 为 `html/body/#app` 补充 `height: 100%`，保证应用根高度链完整。

## 验证

- 运行 `npm run check`。
