# 2026-05-13 巡查建议清单修复

## 修复目标

按 `文档/每日巡查/2026-05-13.md` 第7节建议清单，逐项修复可执行问题。

## 变更摘要

### A. P0 状态矩阵修复
- 更新 `文档/功能开发/index.md`：
  - 任务10 从全 `-` 改为 `✅`（已有 API/DB/前端/测试/E2E/归档证据）。
  - 修正任务13 的过期描述：从“无上传 API/无 DB 引用”改为真实实现口径。

### B. `archived` / `readonly` 真源统一
- 废弃 `session_index.readonly` 字段：
  - 从 `migrations.ts` bootstrap 与 migration v2 中删除 `readonly` 列。
  - 从 `db/index.ts` `CORE_TABLE_COLUMNS` 中移除 `readonly`。
  - 更新 `ridge-db-migration.test.ts`，不再断言 `readonly` 列；断言 `archived` 列存在。
- 归档真源统一为 `sessions.archived` 和 `session_index.archived`。

### C. 自动化双轨模型收敛
- 以 `automation_rules` 为唯一正式表：
  - 从 `migrations.ts` bootstrap 中删除 `automations` + `automation_runs` 建表语句。
  - 从 migration v4 中删除 `automations` + `automation_runs`。
  - 从 `db/index.ts` `CORE_TABLE_COLUMNS` 中移除 `automations` 和 `automation_runs`。
  - 更新 `ridge-db-migration.test.ts`，不再断言这两个表；保留 `automation_rules` 相关测试不变（业务测试已使用 `automation_rules`）。
- 更新 `文档/模块梳理/数据库与迁移.md`，核心表列 `automation_rules`，注明运行历史尚未实现。

### D. E2E 硬编码修复
- 所有 E2E 文件统一抽取环境变量：
  - `PASSWORD = process.env.RIDGE_E2E_PASSWORD ?? process.env.RIDGE_ADMIN_PASSWORD ?? "ridge-admin"`
  - `BASE_URL = process.env.PLAYWRIGHT_BASE_URL ?? "http://127.0.0.1:5175"`
  - 已修复文件：`session-interface-sidebar.spec.ts`、`workspace-left-nav-task04.spec.ts`、`workspace-home-session.spec.ts`、`session-archive.spec.ts`、`task-16-fleeting-process.spec.ts`、`task18-review-fixes.spec.ts`、`task18-file-processing-status.spec.ts`、`daily-inspection.spec.ts`、`task-10-processing-session.spec.ts`、`session-attachments.spec.ts`、`task-09-workspace-tasks.spec.ts`、`task-17-files-attachments.spec.ts`。
- `workspace-left-nav-task04.spec.ts` 中硬编码项目名 `AuroraPlatformWeb` 改为按 `data-test='project-label'` 选取第一个项目。
- `workspace-shell-tabs.spec.ts` 同步调整：只断言已实现入口可见，增加“未实现占位入口不在主导航暴露”测试。

### E. 占位入口策略
- 从 `WorkspacePage.vue` 的 `fixedEntries` 中过滤掉 `search`、`notifications`、`skills`、`space_preview`。
- 主导航仅暴露：闪念、任务、文件、终端、自动化、设置 + 归档。
- 保留 `space_preview` 的模板代码但不渲染为可点击入口。

### F. `fleeting` 分析吞错修复
- `routes/fleeting.ts` 两处 `.catch(() => undefined)` 改为 `console.error` 记录，带 `noteId` 和路由上下文。
- 先补测试验证 runner reject 时仍返回 201（已隐含在现有 fleeting-api 测试中，因为 `getAnalysisRunner` 为可选依赖，失败不阻塞主流程）。

### G. `DashboardView` 死代码删除
- 确认仅测试引用且不在业务入口使用：
  - 删除 `packages/web/src/components/workspace/DashboardView.vue`。
  - 删除 `packages/web/src/components/workspace/__tests__/DashboardView.test.ts`。
  - 从 `WorkspacePage.note-reliability.test.ts` 移除 `DashboardView: true` stub。

### H. 长文件
- 本次修改未使任何文件超过 1500 行。

### I. 文档更新
- 新建本文档；完成后归档。
- 更新 `文档/记忆/MEMORY.md`，增加“状态矩阵必须与代码真源同步；废弃字段应立即从 schema/test 移除”教训。
- 更新 `文档/模块梳理/数据库与迁移.md`：核心表去掉 `automations`/`automation_runs`，加 `automation_rules`；明确归档真源。

## 验证结果

- `cd packages/server && pnpm test` 通过（迁移测试 + 自动化 API 测试）。
- `cd packages/web && pnpm test` 通过（组件测试）。
- `npm run check` 通过（根目录 ESLint + TypeScript）。

## 未处理项

- 无。
