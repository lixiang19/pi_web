# 2026-05-11 巡查修复：P0/P1 代码修复与任务项目绑定

## 目标

按 `文档/每日巡查/2026-05-11.md` 逐项修复和补完。

## 验收项

### A. P0/P1 代码修复

1. **闪念 API 端到端可用**
   - `index.ts` 挂载 `/api/fleeting`，在 `authMiddleware` 之后。
   - `clips` 表进版本化迁移（version 7），删除 `ensureFleetingSchema` 隐式建表。
   - 补 migration/repair columns、迁移测试。

2. **自动化 store/scheduler 初始化修复**
   - `automationStore` 用 lazy getter（`getAutomationStore` 在首次调用时初始化），或重排初始化顺序。
   - 所有 `reschedule()` 调用指向当前非 null scheduler。
   - 补集成测试。

3. **`/api/files/create` 安全边界**
   - 先越界检查再 `mkdir`；拒绝 `../`、绝对路径、`.ridge`。
   - 复用 `file-manager.ts` 的 `ensureWithinRoot` / `assertNotRidgeSystemPath`。
   - 补越界路径集成测试。

4. **归档会话只读闭环**
   - 明确 `sessions.archived` 为真源；归档后发送消息 403。
   - 补服务端测试。
   - 文档说明 `session_index.readonly` 未落地。

5. **首页附件上传失败回滚**
   - 附件上传失败后调用 `deleteSessionApi` 清理刚创建的会话。
   - 补前端测试：失败时调用 delete/cleanup，不 replaceTab。

6. **认证密码配置化**
   - 生产环境读取 `RIDGE_ADMIN_PASSWORD`；非 production/test 允许默认 `ridge-admin`。
   - production 缺少密码拒绝启动或明确报错。
   - 更新测试辅助与 README。

### B. 任务项目绑定与筛选

1. **DB/API/service**
   - `workspace_tasks` / `workspace_milestones` 增加可空 `project_id`，进 migration + repair columns。
   - 创建/更新里程碑支持 `projectId`。
   - 创建任务支持 `projectId`；未显式传且里程碑有 `projectId` 时继承。
   - 列表 API 支持 `projectId=none` / 具体 id / omitted=全部。

2. **前端**
   - `useWorkspaceTasks` 支持项目筛选状态。
   - `TaskView.vue` 增加项目筛选控件（全部、无项目、具体项目）。
   - 创建/编辑可选择项目。

3. **测试**
   - 后端 API 测试：项目继承、筛选、agent 不能完成仍通过。
   - 前端测试覆盖筛选控件/调用参数。

### C. P2 文档与状态同步

- 完成后归档本文档到 `文档/功能开发/归档/`。
- 更新 `index.md` 功能状态矩阵。
- 更新 `数据库与迁移.md`、`ridge系统目录边界.md`、新增/更新任务系统模块梳理。
- 更新 `MEMORY.md`。

### D. 最终验证

- `cd packages/server && pnpm test`
- `cd packages/web && pnpm test`
- 根目录 `npm run check`
