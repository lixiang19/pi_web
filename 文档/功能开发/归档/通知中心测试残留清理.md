# 通知中心测试残留清理

## 背景

真实 `~/.pi/ridge.db` 中出现 6 条 `task18-e2e-*` 文件失败通知。对应文件和处理状态已删除，但 `notification_events` 未随文件删除清理。

## 处理

- 已将现存 `/Users/lixiang/ridge-workspace/task18-e2e-*` 通知标记为 `handled`。
- `DELETE /api/files/entries` 删除文件/目录时，同步删除 `notification_events` 中 `related_type='file'` 且 `related_id` 精确或目录前缀匹配的通知。
- 目录前缀清理复用 `%`、`_`、`\` 转义规则，避免误删相似路径。
- `summary.daily` 读取的 session jsonl 已不存在时，job 标记为 skipped completed，不再重试到失败通知。

## 验证

- `cd packages/server && pnpm test -- file-processing-status.test.ts`
- `cd packages/server && pnpm test -- workspace-memory.test.ts`
