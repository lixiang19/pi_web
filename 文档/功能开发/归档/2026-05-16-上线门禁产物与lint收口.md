# 上线门禁产物与 lint 收口

状态：已归档

## 目标

修复项目巡查发现的上线质量问题：`any` warning 未被收口、临时/测试产物进入源码真源、项目注册文档状态码与实现不一致。

## 已完成

- `release-gate.test.ts` 新增门禁：禁止 `packages/server/src`、`packages/web/src` 下残留 `.tmp` 文件，禁止根目录与 Web 包内 `test-results` 入库，禁止 E2E 继续写 `e2e/screenshots` 固定仓库路径。
- 删除 `packages/web/src/composables/useInbox.ts.tmp`、`test-results/.last-run.json`、`packages/web/test-results/.last-run.json` 和旧的 `packages/web/e2e/screenshots/*.png` 截图产物。
- `.gitignore` 新增 Playwright 输出目录：`test-results`、`packages/web/test-results`、`playwright-report`、`packages/web/playwright-report`、`packages/web/e2e/screenshots`。
- `file-tree.spec.ts` 的手动截图统一写入已忽略的 `test-results/e2e-screenshots/`。
- 清理 `useFileTreeActions`、相关测试、闪念测试、会话索引测试、稳定性脚本中的 lint warning。
- `文档/模块梳理/项目注册与设备绑定.md` 和任务 30/31/32 归档文档同步设备校验状态码：设备不存在返回 404，离线/不可达返回 409。

## 验证

- `pnpm --dir packages/server exec vitest run src/__tests__/release-gate.test.ts`
- `npm run check`
- `pnpm test`
